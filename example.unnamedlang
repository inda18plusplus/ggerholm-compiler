int func(x){
    print((-x * (5 + !5 - 1) + 5 * ~2) / 5);
    return 0;
}

int main(){
    if (1 + 2 - 4 < 0) {
        second(25, 10);
        func(50);
    } else {
        func(50);
    }
    print(2 * 6);

    for (i = 2; i > -5; -1){
        print(i);

        for (j = 0; j < 2; 1){
            i = i - 1;
        }
    }

    return 2 * 4 / 3;
}

int second(x, y){
    print(x = 4 * y);
    x = x - 4;
    print(x);
    return 5;
}